import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  //assetPrefix: process.env.NODE_ENV === 'development' ? undefined : 'https://palmilhasterapeuticas.com.br',
};

export default nextConfig;
